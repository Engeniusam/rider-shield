import React from "react";
import "./BlinkingText.css"; // Import the CSS file

const BlinkingText = () => {
  return (
    <div className="blinking-text">
      Disclaimer: The rider is not covered on either of the two types of cover
      but the passenger or goods are covered.
    </div>
  );
};

export default BlinkingText;
